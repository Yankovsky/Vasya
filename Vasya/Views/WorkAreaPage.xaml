<UserControl x:Class="Vasya.Views.WorkAreaPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <TabControl TabStripPlacement="Left">
        <TabItem Name="NewTopo" Header="new topo" Height="100">
            <TabItem.Content>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <TextBox Grid.Row="1" Text="{Binding Value}" HorizontalAlignment="Center" />
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding MinValue}"></TextBlock>
                        <Slider x:Name="slider" Grid.Column="1" Value="{Binding Value}"
                                Minimum="{Binding MinValue}" Maximum="{Binding MaxValue}">
                        </Slider>
                        <TextBlock Grid.Column="2" Text="{Binding MaxValue}"></TextBlock>
                    </Grid>
                    <ScrollViewer Grid.Row="3" ScrollViewer.VerticalScrollBarVisibility="Hidden">
                        <Image Source="{Binding NewTopoFilteredImage}" Stretch="Uniform"></Image>
                    </ScrollViewer>
                </Grid>
            </TabItem.Content>
        </TabItem>
        <TabItem Name="OriginalTopo" Header="original topo" IsSelected="{Binding OriginalTopoSelected}" Height="100">
            <TabItem.Content>
                <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Hidden">
                    <Image Source="{Binding OriginalTopoFilteredImage}" Stretch="Uniform"></Image>
                </ScrollViewer>
            </TabItem.Content>
        </TabItem>
    </TabControl>
</UserControl>